PORT=3000

# MySQL (XAMPP: mặc định root không mật khẩu — để DB_PASSWORD= trống; nếu đã đặt mật khẩu trong phpMyAdmin thì ghi vào đây)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=123456
DB_NAME=vocab_app

# JWT
JWT_SECRET=your_jwt_secret_key_here
JWT_EXPIRES_IN=7d
